// Code generated by protoc-gen-connect-go. DO NOT EDIT.
//
// Source: proto/greet/greet.proto

package greetconnect

import (
	connect "connectrpc.com/connect"
	context "context"
	errors "errors"
	greet "example.com/aaa/gen/proto/greet"
	http "net/http"
	strings "strings"
)

// This is a compile-time assertion to ensure that this generated file and the connect package are
// compatible. If you get a compiler error that this constant is not defined, this code was
// generated with a version of connect newer than the one compiled into your binary. You can fix the
// problem by either regenerating this code with an older version of connect or updating the connect
// version compiled into your binary.
const _ = connect.IsAtLeastVersion1_13_0

const (
	// GreetServiceName is the fully-qualified name of the GreetService service.
	GreetServiceName = "greet.GreetService"
)

// These constants are the fully-qualified names of the RPCs defined in this package. They're
// exposed at runtime as Spec.Procedure and as the final two segments of the HTTP route.
//
// Note that these are different from the fully-qualified method names used by
// google.golang.org/protobuf/reflect/protoreflect. To convert from these constants to
// reflection-formatted method names, remove the leading slash and convert the remaining slash to a
// period.
const (
	// GreetServiceSayHelloProcedure is the fully-qualified name of the GreetService's SayHello RPC.
	GreetServiceSayHelloProcedure = "/greet.GreetService/SayHello"
)

// GreetServiceClient is a client for the greet.GreetService service.
type GreetServiceClient interface {
	SayHello(context.Context, *connect.Request[greet.GreetRequest]) (*connect.Response[greet.GreetResponse], error)
}

// NewGreetServiceClient constructs a client for the greet.GreetService service. By default, it uses
// the Connect protocol with the binary Protobuf Codec, asks for gzipped responses, and sends
// uncompressed requests. To use the gRPC or gRPC-Web protocols, supply the connect.WithGRPC() or
// connect.WithGRPCWeb() options.
//
// The URL supplied here should be the base URL for the Connect or gRPC server (for example,
// http://api.acme.com or https://acme.com/grpc).
func NewGreetServiceClient(httpClient connect.HTTPClient, baseURL string, opts ...connect.ClientOption) GreetServiceClient {
	baseURL = strings.TrimRight(baseURL, "/")
	greetServiceMethods := greet.File_proto_greet_greet_proto.Services().ByName("GreetService").Methods()
	return &greetServiceClient{
		sayHello: connect.NewClient[greet.GreetRequest, greet.GreetResponse](
			httpClient,
			baseURL+GreetServiceSayHelloProcedure,
			connect.WithSchema(greetServiceMethods.ByName("SayHello")),
			connect.WithClientOptions(opts...),
		),
	}
}

// greetServiceClient implements GreetServiceClient.
type greetServiceClient struct {
	sayHello *connect.Client[greet.GreetRequest, greet.GreetResponse]
}

// SayHello calls greet.GreetService.SayHello.
func (c *greetServiceClient) SayHello(ctx context.Context, req *connect.Request[greet.GreetRequest]) (*connect.Response[greet.GreetResponse], error) {
	return c.sayHello.CallUnary(ctx, req)
}

// GreetServiceHandler is an implementation of the greet.GreetService service.
type GreetServiceHandler interface {
	SayHello(context.Context, *connect.Request[greet.GreetRequest]) (*connect.Response[greet.GreetResponse], error)
}

// NewGreetServiceHandler builds an HTTP handler from the service implementation. It returns the
// path on which to mount the handler and the handler itself.
//
// By default, handlers support the Connect, gRPC, and gRPC-Web protocols with the binary Protobuf
// and JSON codecs. They also support gzip compression.
func NewGreetServiceHandler(svc GreetServiceHandler, opts ...connect.HandlerOption) (string, http.Handler) {
	greetServiceMethods := greet.File_proto_greet_greet_proto.Services().ByName("GreetService").Methods()
	greetServiceSayHelloHandler := connect.NewUnaryHandler(
		GreetServiceSayHelloProcedure,
		svc.SayHello,
		connect.WithSchema(greetServiceMethods.ByName("SayHello")),
		connect.WithHandlerOptions(opts...),
	)
	return "/greet.GreetService/", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {
		switch r.URL.Path {
		case GreetServiceSayHelloProcedure:
			greetServiceSayHelloHandler.ServeHTTP(w, r)
		default:
			http.NotFound(w, r)
		}
	})
}

// UnimplementedGreetServiceHandler returns CodeUnimplemented from all methods.
type UnimplementedGreetServiceHandler struct{}

func (UnimplementedGreetServiceHandler) SayHello(context.Context, *connect.Request[greet.GreetRequest]) (*connect.Response[greet.GreetResponse], error) {
	return nil, connect.NewError(connect.CodeUnimplemented, errors.New("greet.GreetService.SayHello is not implemented"))
}
